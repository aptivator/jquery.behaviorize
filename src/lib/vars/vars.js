import validationConfigs from './lib/validation-configs';
import validators        from './lib/validators';

const actions = {};
const prefix = {prefix: ''};

export {
  actions,
  prefix,
  validationConfigs,
  validators
};
