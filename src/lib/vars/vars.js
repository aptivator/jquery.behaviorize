import validifyConfigs from './lib/validify-configs';
import validators      from './lib/validators';

const actions = {};
const prefix = {prefix: '$'};
const transforms = {};

export {
  actions,
  prefix,
  transforms,
  validifyConfigs,
  validators
};
